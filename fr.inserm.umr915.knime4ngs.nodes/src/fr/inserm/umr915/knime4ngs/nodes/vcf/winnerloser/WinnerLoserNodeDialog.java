package fr.inserm.umr915.knime4ngs.nodes.vcf.winnerloser;

import org.knime.core.node.defaultnodesettings.DefaultNodeSettingsPane;




public class WinnerLoserNodeDialog extends DefaultNodeSettingsPane
	{
	
    protected WinnerLoserNodeDialog()
    	{
    	/*
    	DataTypeColumnFilter filter=new DataTypeColumnFilter(StringCell.TYPE);
    	addDialogComponent(new DialogComponentColumnNameSelection(
    			new SettingsModelString(WinnerLoserNodeModel.GENE_COLUMN_PROPERTY, WinnerLoserNodeModel.DEFAULT_GENE_COLUMN)
    			"Column for Gene", 0, filter));
    	
    	addDialogComponent(new DialogComponentNumber(
                new SettingsModelInteger(
                	WinnerLoserNodeModel.MIN_SAMPLE_PER_SNP_PROPERTY, WinnerLoserNodeModel.DEFAULT_MIN_SAMPLE_PER_SNP),
                    "Minimum Sample per mutation",1));
    	addDialogComponent(new DialogComponentNumber(
                new SettingsModelInteger(
                	WinnerLoserNodeModel.MAX_SAMPLE_PER_SNP_PROPERTY, WinnerLoserNodeModel.DEFAULT_MAX_SAMPLE_PER_SNP),
                    "Maximum Sample per mutation",1));
                   */
    	}
	}

